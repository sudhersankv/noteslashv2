# Root environment template
# Copy per-app files — do not put secrets in this file at repo root.
#
#   cp backend/.env.example backend/.env
#   cp frontend/.env.example frontend/.env.local
#
# Never commit .env or .env.local
