# Noteslash API

FastAPI backend: ingestion, pgvector retrieval, chat, voice sessions, reports.

See the [root README](../README.md) for setup and deployment. Deploy with root directory `backend` on Railway (or similar).

```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000
```
