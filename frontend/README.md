# Frontend

Next.js 14 (App Router) + TypeScript + Tailwind.

shadcn/ui will be added during the UI build phase.

## Setup

```bash
cp .env.example .env.local
npm install
npm run dev
```

## Deploy (Vercel)

- Root directory: `frontend`
- Environment: `NEXT_PUBLIC_API_URL` → production backend URL
